// src/repositories/ProductRepository.js
class ProductRepository {
    save(product) {
      console.log(`Produk ${product.name} disimpan ke database.`);
      // Simulasi penyimpanan
    }
}
  
export default ProductRepository;  